---
layout:     post   				    # 使用的布局（不需要改）
title:      字符串				# 标题 
subtitle:    #副标题
date:       2024-10-07 				# 时间
author:     By xrj						# 作者
header-img: img/post-bg-2015.jpg 	#这篇文章标题背景图片
catalog: true 						# 是否归档
tags:								#标签
    - 算法
    - 字符串
    - 力扣
---

```java
    // String常用方法
    s.charAt(int index);
    s.indexOf(char ch);
    s.indexOf(String str); // 返回子字符串所在索引
    char[] ch = str.toCharArray(); // 转换为char数组
    s.toUpperCase(),s.toLowerCase();
    s.contains(String str);
    s.substring(int start, int end); // 注意全小写
    s1.equals(String s2);
    String[] arr = s.split(char ch); // 按照字符分割，返回新的String数组
    s1.concat(s2); // 连接两个字符串，常用“+”
```

```java
    // String类是不可改变的，StringBuilder速度快但是线程不安全,StringBuffer类可改变且线程安全
    // StringBuffer常用方法
    StringBuffer sb = new StringBuffer(10); // 创建一个容量为10的空容器
    sb.append(String s);
    sb.delete(int start,int end); // 左闭右开
    sb.deleteCharAt(int index); // 从index删除后面全部
    sb.insert(int index,String str);
    sb.replace(int start,int end,String str);

    // StringBuilder常用方法
    sb.setcharAt(int index, char ch);
```

## 1. 反转字符串II（541）

1. java求长度

    数组是length，String类是length(),泛型集合是size()。
    ```java
    char[] ch = new char[20];
    ch.length;
    String s = "Hello";
    s.length();
    List<Integer> list = new ArrayList<>();
    list.size();
    ```



## 2. 找出字符串中第一个匹配项的下标（28）

1. KMP算法

    前缀和构建next数组

    ```java
    // 初始化
    // 采用-1的方式存储next数组，value直接对应下标
    public void getNext(int[] next, String s){
        int j =-1;
        next[0] = -1;

        for(int i=1;i<s.length();i++){
            while(j>=0 && s.charAt(i) != s.charAt(j+1)){
                j = next[j];
            }
            if(s.charAt(i) == s.charAt(j+1)) j++;
            next[i] = j;
        }
    }
    ```

2. 根据next数组寻找匹配项

    仿照构建next数组的方法，逐步匹配needle和haystack
    ```java
    int j=-1;

    for(int i=0;i<haystack.length();i++){
        while(j>=0 && haystack.charAt(i) != needle.charAt(j+1)){
            j =next[j];
        }
        if(haystack.charAt(i) == needle.charAt(j+1)) j++;
        if(j == needle.length()-1) return i-j;
    }
    ```

## 3. 重复的子字符串（459）

1. 移动匹配

    假如存在重复的子串，可以利用s+s，然后删除头尾元素进行判断。
    ```java
    StringBuilder sb = new StringBuilder(s+s);   
    sb.deleteCharAt(0);
    sb.deleteCharAt(sb.length()-1);

    return sb.toString().contains(s);
    ```

2. KMP算法

    主要难点在于证明
    ```java
    if(next[len-1]>=0 && len % (len-next[len-1]-1) == 0) return true;
    ```